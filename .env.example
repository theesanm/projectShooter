# Environment variables for game configuration

# Movement Mode: 'full' or 'horizontal'
# full - Player can move in all directions (up, down, left, right)
# horizontal - Player can only move left and right (enemies approach from top)
VITE_MOVEMENT_MODE=horizontal

# API Base URL (currently disabled - for future database integration)
# VITE_API_URL=http://localhost:3001/api
# VITE_API_URL=https://your-production-api.com/api
